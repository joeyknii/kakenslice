# Image Management

Replace images in these folders without changing core HTML/CSS/JS:

- `hero/` for homepage hero banner image
- `about/` for about/chef photo
- `cakes/` for cake catalog images

Then update `config.js` image paths if filenames change.
